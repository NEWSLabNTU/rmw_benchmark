# Custom Configuration Template
# Copy this file to custom.conf and modify for your specific needs
# Use: cp custom.conf.example custom.conf
#      Edit custom.conf with your parameters
#      Set STRESS_CONFIG=custom.conf in Makefile

# =============================================================================
# Video Parameters
# =============================================================================
# Adjust to your needs - data rate = WIDTH × HEIGHT × 3 bytes × FPS
WIDTH=2560
HEIGHT=1440
FPS=45

# =============================================================================
# GStreamer Pipeline
# =============================================================================
# Variables {WIDTH}, {HEIGHT}, {FPS} will be substituted by Makefile
#
# Example patterns:
# - videotestsrc pattern=smpte (default SMPTE color bars)
# - videotestsrc pattern=snow (random noise)
# - videotestsrc pattern=ball (moving ball)
# - videotestsrc pattern=checkers-8 (checkerboard)
#
# You can also customize the entire pipeline:
# GSTREAMER_PIPELINE="videotestsrc pattern=ball ! video/x-raw,width={WIDTH},height={HEIGHT},framerate={FPS}/1 ! videoconvert"
GSTREAMER_PIPELINE="videotestsrc pattern=smpte ! video/x-raw,width={WIDTH},height={HEIGHT},framerate={FPS}/1 ! videoconvert"

# =============================================================================
# QoS (Quality of Service) Settings
# =============================================================================

# Reliability
# - RELIABLE: Guarantees delivery, retransmits lost messages (recommended)
# - BEST_EFFORT: Best-effort delivery, may drop messages for better performance
QOS_RELIABILITY=RELIABLE

# Durability
# - VOLATILE: Data not stored, only for active subscribers (typical for video)
# - TRANSIENT_LOCAL: Late-joining subscribers get recent historical data
QOS_DURABILITY=VOLATILE

# History
# - KEEP_LAST: Keep only the last N messages (specified by depth)
# - KEEP_ALL: Keep all messages (subject to resource limits)
QOS_HISTORY=KEEP_LAST

# History Depth (for KEEP_LAST)
# Number of messages to keep in history
# Larger values use more memory but provide more buffering
QOS_HISTORY_DEPTH=10

# Deadline (milliseconds)
# Maximum time between published messages
# 0 = disabled (no deadline constraint)
# Example: 100 means messages must arrive within 100ms
QOS_DEADLINE_MS=0

# Liveliness
# - AUTOMATIC: System automatically maintains liveliness (recommended)
# - MANUAL_BY_TOPIC: Application must assert liveliness per topic
# - SYSTEM_DEFAULT: Application must assert liveliness per node
QOS_LIVELINESS=AUTOMATIC

# Liveliness Lease Duration (milliseconds)
# Maximum time to wait for liveliness assertion
# 0 = infinite
# Set > 0 to detect dead publishers/subscribers
QOS_LIVELINESS_LEASE_MS=0

# =============================================================================
# Notes
# =============================================================================
# Data rate estimates (uncompressed RGB):
# 640x480 @ 15 FPS    = ~14 MB/s
# 1280x720 @ 30 FPS   = ~80 MB/s
# 1920x1080 @ 30 FPS  = ~180 MB/s
# 2560x1440 @ 45 FPS  = ~500 MB/s
# 3840x2160 @ 30 FPS  = ~720 MB/s
